# Normalize line endings so Windows and Unix checkouts don't produce
# whole-file diffs. Text is stored LF in the repo.
* text=auto eol=lf

# Binary assets, never touched
*.png binary
*.jpg binary
*.mp4 binary
